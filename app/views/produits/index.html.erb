<div class="container">
  <div class="row ">
      <% @produits.each do |produit| %>
      <div class="col-sm-3 col-xs-6">
        <div class="card text-center">
          <div class="card-pic">
            <%= cl_image_tag produit.photo, crop: :thumb, class: "img-responsive" %>
          <div class="card-hover">
            <h4>Voir le produit</h4>
          </div>
          </div>
          <div class="card-info">
            <h5><%= produit.nom %></h5>
            <h6>  <%= produit.prix %> €</h6>
            <%#= simple_form_for @order_item do |f| %>
              <%#= f.hidden_field :produit_id, value: produit.id %>
              <%#= f.input :quantity, collection: 1..10, label: false %>
              <%#= f.button :submit, value: "Ajouter au panier",class: "btn btn-primary btn-block" %>
            <%#end %>
        </div>
      </div>
    </div>
      <% end %>
  </div>
</div>
